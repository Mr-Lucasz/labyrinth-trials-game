// Sequência de Formas - Fase 2
import BasePuzzle from '../BasePuzzle';
export default class FormSequencePuzzle extends BasePuzzle {
    constructor(scene) {
        super(scene);
        // ...implementação específica...
    }
}
