// Puzzle das Sombras - Fase 3
import BasePuzzle from '../BasePuzzle';
export default class ShadowPuzzle extends BasePuzzle {
    constructor(scene) {
        super(scene);
        // ...implementação específica...
    }
}
