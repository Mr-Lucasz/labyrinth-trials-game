// Ordem Numérica - Fase 1
import BasePuzzle from '../BasePuzzle';
export default class NumberSequencePuzzle extends BasePuzzle {
    constructor(scene) {
        super(scene);
        // ...implementação específica...
    }
}
